<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">
    <!--        配置文件要扫描的包
1.component-scan 要对指定包下的类进行扫描，并创建对象到容器
2.base-package 指定要扫描的包
3.含义是当spring容器创建/初始化时，就会扫描component包下的所有的 有注解的，将其实例化，生成对象放入容器
4.resource-pattern="User*.class" 表示只扫描component 和它的子包下的User打头的类
-->
<!--    需求:如果我们希望排除某个包/子包下的某种类型的注解，可以通过exclude-filter来指定
        1.context:exclude-filter 指定要排除哪些类-->
<!--    2.type 指定排除方式 annotation表示按照注解来排除-->
<!--    3.expression="org.springframework.stereotype.Service" 指定要排除的注解的全路径-->
    <!-- 纯注解扫描 -->
    <context:component-scan base-package="component">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service"/>
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"/>
    </context:component-scan>
<!--    1.use-default-filters="false”:不再使用默认的过滤机制
        2.context:include-filter:表示只是扫描指定的注解的类
        3. expression="org.springframework.stereotype.Controller": 注解类名-->
<!--    需求:如果我们希望按照自己的规则，米扫描包/子包下的某些注解，可以通过 include-fiter-->
<!--    1.use-default-filters="false” 表示不使用默认的过滤机制/扫描机制-->
<!--    2.context:include-filter 表示要去扫描哪m  些类-->
<!--    3.type="annotation”按照注解方式来扫描/过滤-->
<!--    4.expression="orgIspringframework.stereotype.Service" 指定要扫描的注解的全路径-->
    <context:component-scan base-package="com.hspedu.spring.component" use-default-filters="false">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Service"/>
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Repository"/>
    </context:component-scan>
</beans>

